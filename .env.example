# ===========================================
# MINIMAL BOOTSTRAP CONFIGURATION
# ===========================================
# All environment variables are managed through the UI at /settings/environment
# 
# OPTION 1: No .env file needed!
# Just run 'pnpm dev' and the app will auto-generate an encryption key
#
# OPTION 2: For production or explicit key control
# Copy this file to .env and set your own encryption key:

# Generate a secure key with: openssl rand -hex 32
ENCRYPTION_KEY=

# ===========================================
# That's it! After starting the app:
# 1. Navigate to http://localhost:3000/settings/environment
# 2. Add all your credentials through the secure web interface
# ===========================================